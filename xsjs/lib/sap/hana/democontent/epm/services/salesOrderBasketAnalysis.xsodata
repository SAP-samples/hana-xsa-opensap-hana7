service namespace "com.sap.openSAP.hana.example.services"  { 
   "core.models::SALESORDER_BASKET_ANALYSIS" 
    as "SalesBasket" with ("CATEGORY", "SALES")   
	keys generate local "ID"
    aggregates always;	  
}